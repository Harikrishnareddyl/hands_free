.DS_Store
xcuserdata/
*.xcodeproj/
build/
DerivedData/
.build/
.swiftpm/
Package.resolved
*.xcworkspace/
!default.xcworkspace/
*.hmap
*.ipa
*.dSYM.zip

# DMGs built locally — release artifacts are attached via GitHub Actions
*.dmg

# Icon intermediates (regenerable via scripts/make-icns.sh)
scripts/.icon-build/
HandsFree/Resources/AppIcon.iconset/

# Local secrets
.groq-key

.env

# Local wake-word model backups — the active ONNX ships in HandsFree/Resources/,
# any "_v1" / "_test" variants are local debug/comparison artifacts that
# shouldn't be in the repo.
hey_aira_v1.onnx
HandsFree/Resources/hey_aira_v1.onnx
HandsFree/Resources/hey_aira_test.onnx
