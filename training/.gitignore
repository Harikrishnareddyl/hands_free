# Local runs (shouldn't happen on your laptop — this is Modal-only — but just
# in case you poke around with livekit-wakeword directly).
data/
output/
*.onnx
__pycache__/
*.pyc
.venv/

# Voice recording work — raw takes and split clips stay local. We upload
# them to Modal for fine-tuning, not to git.
voice/raw/
voice/clips/
!voice/raw/.gitkeep
!voice/clips/.gitkeep
